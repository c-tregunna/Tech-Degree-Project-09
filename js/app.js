// Header carosel

// $(document).ready(function(){
//     $('.carosel').slick({
//         $('.single-item').slick();
//     });
//   });

$('.slides').slick({
    dots: true,
    autoplay: true,
    arrows: false,
    autoplaySpeed: 4000,
    fade: true
  });